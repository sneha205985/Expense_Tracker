# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9  # PostgreSQL driver (Phase 3)

# Authentication (Phase 4)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6  # For form data

# Environment variables
python-dotenv==1.0.0

# Pydantic (included with FastAPI but explicit)
pydantic==2.5.0
pydantic-settings==2.1.0

# GUI dependencies (existing)
matplotlib==3.8.2

# HTTP client for GUI (Option A)
requests==2.31.0
